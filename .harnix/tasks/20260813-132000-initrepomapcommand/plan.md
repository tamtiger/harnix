# Plan

1. Add RED integration assertions for the init cache and public short map query/help.
2. Refresh the cache only after successful fresh init; include its logical path in init output and dry-run planning.
3. Add `harnix repo-map --query <text>` as public cache-only JSON-default query with the existing bounds and internal implementation.
4. Update public-command contracts/docs, version and changelog; run focused then full gates.
